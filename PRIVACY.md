# JEV 书签整理隐私政策

生效日期：2026 年 9 月 21 日。

JEV 书签整理是一款用户自带 TypeSafe API Key 的 Chrome 扩展。管理页在本机读取 Chrome 书签树的标题、URL、文件夹结构和位置，用于展示范围与生成迁移预览；只有用户选择范围内的网页书签会进入批量分类请求。用户确认后，扩展使用 Chrome 书签 API 移动选中书签。用户新增网页书签时，扩展可在开启该选项的情况下给出目标目录建议。

## 数据发送

用户触发批量分类，或开启新收藏建议后新增网页书签时，扩展直接向 `https://api.typesafe.ai/v1/systemone` 发送该书签的**标题与 URL**、用户配置的候选目录名称及用途说明、用户自己的 API Key。TypeSafe 返回类别、概率和置信度。扩展不向开发者运营的服务器发送书签或密钥；不会读取或上传浏览历史、页面正文、Cookie、密码或打开的标签页内容。TypeSafe 对其收到的信息的处理受其自身条款与隐私政策约束。

用户点击“测试连接”时，会用输入的 Key、当前目录模板和固定的测试标题、测试 URL（`https://example.com/`）向 TypeSafe 发起一次分类请求；不会发送真实书签。

## 本机存储

API Key 仅存于内存中的 `chrome.storage.session`，关闭浏览器后需重新输入；目录模板、设置、待处理建议和最近一次批量整理的撤销记录存于本机 `chrome.storage.local`。升级自旧版本时，扩展首次读取设置会将旧的本机 Key 移至会话存储并从本机设置中移除；Chrome 本机存储并不提供加密保证，不应将旧存储当作安全密钥库。扩展不使用 `storage.sync`。用户可在设置里修改 Key 或停用新增建议；卸载扩展会删除其扩展存储。撤销记录可通过“保留当前布局”清除。Chrome 浏览器自身的书签同步功能若已开启，用户确认后的书签改动可能按浏览器设置同步到其 Google 账号。

扩展对从 Chrome API 获得的信息的使用和传输遵守 Chrome Web Store 用户数据政策，包括 Limited Use 要求。扩展仅将这些数据用于用户请求的书签分类、移动、撤销与新收藏建议，不出售数据，也不将数据用于广告、画像、分析或其他无关目的。除完成用户请求所需的 TypeSafe 分类调用外，不向第三方传输用户数据；不允许人工读取用户数据，法律要求或用户明确授权的支持请求除外。

## 权限用途

- `bookmarks`：读取选定书签并在用户确认后创建目标文件夹、移动书签和撤销本次移动。
- `storage`：在本机保存设置、建议和撤销记录。
- `notifications`：可选地提醒用户处理新书签建议；用户可在设置中关闭。
- `https://api.typesafe.ai/*`：使用用户自己的 Key 直接调用 Jev 分类 API。

本扩展没有广告、分析 SDK 或开发者运营的用户账号。隐私相关问题可通过其 Chrome Web Store 页面列出的开发者支持方式联系。
